name: pixelcrafts_platform
description: PixelCrafts platform SDK — unified API client for auth, billing, support, sync, legal, and push.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
