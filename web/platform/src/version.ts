/**
 * PixelCrafts Platform SDK Version
 *
 * Bump this version when releasing a new build.
 * Projects can import this to verify which SDK version is running.
 */
export const SDK_VERSION = "1.0.0";

/** Build timestamp (set at compile time) */
export const SDK_BUILD_TIME = new Date().toISOString();
